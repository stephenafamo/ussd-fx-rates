{
  "require": {
    "php": "^7.0.0",
    "guzzlehttp/guzzle": "^6.2"
  },
  "scripts": {
    "compile": [
      "touch current_rates.json && chmod -R 777 current_rates.json",
      "touch last_timestamp && chmod -R 777 last_timestamp"
    ]
  }
}